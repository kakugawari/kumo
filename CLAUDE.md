# くもスケッチ

空に雲を描く、静かなスケッチ帳。単一 HTML の PWA (GitHub Pages で main から公開)。
進め方と「踏んだ落とし穴」の表は kakugawari/app_template の CLAUDE.md に従う。

## 動かす・試す

```bash
python3 -m http.server 8080   # http://localhost:8080/
npm run test:ui               # ブラウザで動かす見張り (要 playwright)
```

## 決めごと

- **対象の端末は iPhone 16 Plus だけ。** ほかの画面幅は見ない
- **方向は癒し寄り。** 遊びの要素 (図鑑・アルバムなど) はあとからゆるく足す
- **空は自動で合わせない。** 使う人が実際の空を見ながら、色も太陽も雲も自分の手で写し取る (空の写生)
- **雲そのものは計算で描く** (太陽に合わせて陰影が変わるのが強み)。
  イラストをもらうのは、地上の景色・鳥などの小物・アイコン・ストア用の画像
- 版を上げるときは `index.html` の `VER` と `sw.js` の `CACHE` を同じ番号にする
  (見張りが確かめる)。版はロゴ → メニューのいちばん下に出る

## 作りのメモ

- 雲1つごとに「形 (濃さ `pc.D`)」と「塗り (`pc.img`、後光込み)」を取っておく。
  描き直しは塗りを貼るだけ。太陽や色が変わったら塗りだけ、1コマ 8ms ずつ塗り直す
- 後光 (ぼかし) は雲の小さな絵の上で1度だけ付ける。画面いっぱいの層で `filter` を使わない
- 元に戻すは、ひとなぞり (`s.g`) ごと
